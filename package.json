{
  "name": "web-adb-inspector",
  "version": "1.0.0",
  "description": "Browser-based ADB device inspector using WebUSB — like GTS EdiHost device info",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@trystero-p2p/ws-relay": "^0.25.3",
    "@yume-chan/adb": "^2.6.0",
    "@yume-chan/adb-credential-web": "^2.1.0",
    "@yume-chan/adb-daemon-webusb": "^2.3.2",
    "@yume-chan/stream-extra": "^2.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
